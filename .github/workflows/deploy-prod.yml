name: Deploy Production — chat.alcruxia.com

on:
  push:
    branches: [prod]

jobs:
  build:
    name: Build & Push imagen Docker
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
    steps:
      - name: Checkout con historial completo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login a GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Metadata de la imagen
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/alcruxia/chatwoot
          tags: |
            type=raw,value=prod
            type=sha,prefix=prod-

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build y push imagen
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.prod
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=prod
          cache-to: type=gha,mode=max,scope=prod

  deploy:
    name: Deploy al servidor
    runs-on: ubuntu-latest
    needs: build
    permissions:
      packages: read
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          envs: GITHUB_TOKEN,GITHUB_ACTOR,GITHUB_SHA
          script: |
            # Login a GHCR
            echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin

            # Clonar o actualizar el repo
            if [ -d ~/alcruxia-omnicanalidad-repo ]; then
              cd ~/alcruxia-omnicanalidad-repo
              git fetch origin prod
              git reset --hard origin/prod
            else
              git clone --branch prod https://x-access-token:${GITHUB_TOKEN}@github.com/Alcruxia/Alcruxia-Omnicanalidad.git ~/alcruxia-omnicanalidad-repo
              cd ~/alcruxia-omnicanalidad-repo
            fi

            # Copiar archivos de configuración del servidor (no están en el repo)
            cp ~/alcruxia-omnicanalidad/.env.prod ~/alcruxia-omnicanalidad-repo/.env.prod
            mkdir -p ~/alcruxia-omnicanalidad-repo/caddy
            cp ~/alcruxia-omnicanalidad/caddy/Caddyfile ~/alcruxia-omnicanalidad-repo/caddy/Caddyfile

            # Deploy
            cd ~/alcruxia-omnicanalidad-repo
            docker compose -f docker-compose.prod.yaml --env-file .env.prod pull
            docker compose -f docker-compose.prod.yaml --env-file .env.prod up -d --remove-orphans
            docker compose -f docker-compose.prod.yaml exec -T rails bundle exec rails db:chatwoot_prepare || true

            # Limpieza
            docker logout ghcr.io
            docker image prune -f
            echo "Deploy completado: $(date)"
